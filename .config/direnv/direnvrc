function use_env_dir() {
    local dir="${1:-.direnv/envs}"
    if [[ -d $dir ]]; then
        for f in $(ls "$dir"); do
            if [[ -f $dir/$f ]]; then
                watch_file "$dir/$f"
                export "$f=$(cat "$dir/$f")"
            fi
        done
    fi
}
